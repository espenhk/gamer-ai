# CarRacing training parameters.

game: car_racing              # game module to use
speed: 1.0
in_game_episode_s: 20.0       # CarRacing default is 1000 steps (~20 s at 50 fps)
n_sims: 100
mutation_scale: 0.05
mutation_share: 1.0
adaptive_mutation: true
patience: 0

policy_type: genetic

policy_params:
  population_size: 20
  elite_k: 3

# n_lookahead_points: 3      # override the number of lookahead waypoints in the observation
# lookahead_step_spacing: 10 # track-checkpoint-index spacing between lookahead waypoints
